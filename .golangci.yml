run:
  timeout: 5m
  tests: true
linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - ineffassign
    - typecheck
    - unused
    - gofumpt
linters-settings:
  gofumpt:
    extra-rules: true
issues:
  max-issues-per-linter: 0
  max-same-issues: 0 